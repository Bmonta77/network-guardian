# Copy this file to .env.local when the app needs environment variables.
# Keep secrets in .env.local and document only safe placeholder values here.
#
# Example:
# DATABASE_URL=
# NEXT_PUBLIC_APP_URL=http://localhost:3000
